# Docker PrestaShop Starter

## Install

    mkdir project_directory
    cd project_directory
    sh -c "$(wget https://raw.githubusercontent.com/phpguild/docker-prestashop-starter/master/install.sh -O -)"
